package util;

/**
 * A simple way of turing debuggin on and off. Used with an if statement. I.e.
 *      if (MyDebugger.ON){ Log This };
 */
public class MyDebugger {
    public static final boolean ON = false;
}
